import React from 'react';

import {
    StyledHeader,
    GithubIco
} from './Header.styled';

export const Header = () => {
    return (
        <StyledHeader>
            <GithubIco />
        </StyledHeader>
    );
};